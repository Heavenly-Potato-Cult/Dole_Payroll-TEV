<?php
// This file creates a symlink from Railway's expected structure to Laravel's public directory
$target = __DIR__ . '/../../public';
$link = __DIR__ . '/public';

if (!is_link($link)) {
    symlink($target, $link);
}

// Include the actual Laravel index.php
require $target . '/index.php';
